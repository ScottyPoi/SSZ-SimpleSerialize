function CreateNode(props) {
  return {
    indexxxx: props.index,
    type: props.type,
    data: props.data,
    isRoot: props.isRoot,
    children: props.children,
    parent: props.parent,
  };
}
